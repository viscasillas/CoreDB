// ShamQL - A minimal SQL-like API for CoreDB.
// -------------------------------------------
// Allows talking to CoreDB as if it was SQL.

{
	user -> coredb[user.id]
	user -> coredb[user.id]
	user -> coredb[user.id]
	user -> coredb[user.id]
}